version: "3"
networks:
  annotatorweb:
    external: false

services:
  development:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: annotator-frontend-client-development
    ports:
      - "60142:60142"
    networks:
      - annotatorweb