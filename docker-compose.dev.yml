version: "3"
networks:
  annotatorweb-dev:
    external: false

services:
  development:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: annotator-frontend:dev
    container_name: annotator-frontend-dev
    ports:
      - "60143:60143"
    networks:
      - annotatorweb-dev