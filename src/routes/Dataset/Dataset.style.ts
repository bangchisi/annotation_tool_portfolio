import { styled } from '@mui/material';

export const Container = styled('div')(() => {
  return {
    padding: '27px',
  };
});

export const Content = styled('div')(() => {
  return {
    //
  };
});
