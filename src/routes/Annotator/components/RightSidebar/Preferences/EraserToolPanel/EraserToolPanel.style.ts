import { Box, TextField, Typography, styled } from '@mui/material';

export const Container = styled('div')(() => {
  return {
    width: '100%',
    height: '100%',
  };
});

export const ToolName = styled(Typography)(() => {
  return {};
});

export const PrefName = styled(Typography)(() => {
  return {};
});

export const EraserRadiusContainer = styled(Box)(() => {
  return {};
});

export const EraserRadius = styled(TextField)(() => {
  return {};
});
