import { styled } from '@mui/material';

export const Container = styled('div')(() => {
  return {
    backgroundColor: '#E7ECF0',
    flex: 1,
    position: 'relative',
    overflow: 'hidden',
  };
});
