import { styled } from '@mui/material';

export const Container = styled('div')(() => {
  return {
    display: 'flex',
    width: '100%',
    height: '100%',
    // maxHeight: '100vh - 50%',
  };
});
